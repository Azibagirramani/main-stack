import { extendTheme } from "@chakra-ui/react";

export const colors = {
  gray: {
    50: "rgba(239, 241, 246, 1)",
    400: "rgba(86, 97, 107, 1)",
  },
};

export const theme = extendTheme({
  colors,
});
